# OceanTrust AI — Python Runtime Dependencies
# ============================================
# This file pins the core runtime packages for pip-based environments.
# For full dependency management (including dev and viz extras) see pyproject.toml.
#
# Install:
#   pip install -r requirements.txt
#
# Streamlit UI:
#   Append `streamlit` is included below so that the UI can be launched
#   without requiring the full pyproject.toml optional extras workflow.

# ── Streaming / Messaging ────────────────────────────────────────────────────
aiokafka>=0.11.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
structlog>=24.2.0

# ── Vector Database ──────────────────────────────────────────────────────────
qdrant-client[async]>=1.9.0

# ── LLM & Embedding APIs ─────────────────────────────────────────────────────
google-generativeai>=0.8.0
langchain-core>=0.2.0
langchain-google-genai>=1.0.10
langchain-text-splitters>=0.2.0
httpx>=0.27.0

# ── LangGraph Agent Orchestration ────────────────────────────────────────────
langgraph>=0.1.0

# ── Streamlit UI ─────────────────────────────────────────────────────────────
streamlit
plotly>=5.22.0
pandas>=2.2.0
watchdog>=4.0.0
